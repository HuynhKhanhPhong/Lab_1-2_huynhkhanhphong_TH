<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/random_root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".RandomScreenActivity">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="MÀN HÌNH RANDOM MÀU"
        android:textSize="22sp"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:layout_marginBottom="40dp"
        android:layout_marginTop="20dp"/>

    <ImageView
        android:id="@+id/iv_random_trigger"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerInParent="true"
        android:layout_toStartOf="@id/iv_call_trigger"
        android:layout_marginEnd="30dp"
        android:src="@android:drawable/ic_menu_rotate"
        app:tint="@android:color/white"
        android:contentDescription="Random Icon"/>

    <ImageView
        android:id="@+id/iv_call_trigger"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_call"
        app:tint="@android:color/white"
        android:contentDescription="Call Icon"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/iv_random_trigger"
        android:layout_marginTop="30dp"
        android:text="Bấm icon quay để đổi màu. Bấm icon điện thoại để gọi."
        android:textSize="14sp"
        android:textStyle="italic"
        android:textColor="@android:color/white"/>

</RelativeLayout>